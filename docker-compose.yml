services:
  mongodb:
    image: mongo:7.0
    container_name: ats_mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  redis:
    image: redis:7.2-alpine
    container_name: ats_redis
    ports:
      - "6380:6379"

volumes:
  mongodb_data:
